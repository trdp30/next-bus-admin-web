export interface LoginPageProps {
  // Add any props that the Login page might receive
}

export interface LoginFormData {
  // Add form data structure if needed
}

export interface LoginError {
  code: string;
  message: string;
}
